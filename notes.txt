
10-09-2020
----------
Не запускалось приложение пока я в функции public MainWindow() не закоментил все вещи после InitializeComponent();
Там не нравились вещи связанные с езернетом.

Какие-то проблемы с локалью, по этому когда номер порта берётся из окна на панели программы, там оказывается не номер порта а бред. НЕТ!!!
Возможно проблема в том что пока не пропингуем нашу цель - вызываются ошибки отправки в блок пакетов и из-за этого эсцепшены.

InitializeComponent();
-------------------------


MainWindow.xaml.cs
------------------
Timer2_Tick - таймер контроля обмена с 330 кассетой , там же шлём запрос статуса кассеты

Panel_INFO.xaml.cs
------------------
Timer1_Tick - таймер обновления данных на панели с каналами питания